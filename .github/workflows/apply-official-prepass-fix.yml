name: Apply official prepass repair

on:
  pull_request:
    branches: [main]
    types: [reopened]

permissions:
  contents: write

jobs:
  patch-verify-publish:
    if: ${{ github.head_ref == 'agent/fix-black-mrt-ssr' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-black-mrt-ssr
          fetch-depth: 0

      - run: python scripts/apply-official-prepass-fix.py
      - run: python scripts/apply-debug-rebuild-fix.py

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: pnpm install --no-frozen-lockfile
      - run: pnpm format
      - run: pnpm verify
      - run: pnpm exec playwright install --with-deps chromium
      - name: Verify visible final and Beauty outputs
        shell: bash
        run: |
          set +e
          pnpm test:e2e --grep "WebGL 2 medium mode renders visible final pixels" 2>&1 | tee official-prepass-e2e.log
          status=${PIPESTATUS[0]}
          exit $status
        env:
          CI: true

      - name: Upload prepass diagnostics
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: official-prepass-diagnostics
          if-no-files-found: ignore
          path: |
            official-prepass-e2e.log
            test-results
            playwright-report

      - name: Commit verified repair and remove bootstrap files
        shell: bash
        run: |
          rm .github/workflows/apply-official-prepass-fix.yml scripts/apply-official-prepass-fix.py scripts/apply-debug-rebuild-fix.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/viewer/src/KyxosViewer.ts packages/viewer/src/scene/createDefaultScene.ts tests/e2e/playground.spec.ts .github/workflows/apply-official-prepass-fix.yml scripts/apply-official-prepass-fix.py scripts/apply-debug-rebuild-fix.py
          git commit -m "fix: separate beauty and rebuild debug passes"
          git push origin HEAD:agent/fix-black-mrt-ssr
